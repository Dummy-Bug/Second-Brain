### Functional Requirements

1. Show autocomplete type ahead result for a prefix.
2. Show top 10 result by frequency of the result.
3. Show results only when `3<= length(prefix) <=20`
4. Result should be personalized(Additional requirement ,ML problem)

> so there are two type of queries here first is when a user type something on google address bar this is called google Type ahead query and other is when a user click on one of the listed options then this is recorded as one successfull search and is called a Search query




